# Dotfile for Mint Linux

## Auto install
* Starship prompt
* Rust + Cargo
* LSD
* Kitty terminal
* Micro
* Aliases

## How to install

* Simply run `./setup`
* Edit `aliasconfig` in `custom.aliases.sh` to point at the correct location 